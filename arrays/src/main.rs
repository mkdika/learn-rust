fn main() {
    // array declaration
    let arr = [1,2,3,4,5];

    // array with types and size
    let a: [i32; 4] = [1,2,3,4];

    let first = a[0];
    let second = a[1];
    println!("first is {}, second is {}", first, second);
}
